import LoadingScreen from '@/components/LoadingScreen'

export default function PublicLoading() {
  return (
    <LoadingScreen
      title="Loading public view..."
      description="Getting the landing, login, or setup experience ready."
    />
  )
}
